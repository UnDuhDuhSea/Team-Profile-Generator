// Required JS Sheets
const Employee = require('./Employee');
const Engineer = require('./Engineer');
const Intern = require('./Intern');
const Manager = require('./Manager');
// Required Packages
const fs = require('fs');


// function to create HTML file

const generateHTML = (Employee, Engineer, Intern, Manager) => {
    // possibly feed these through string template literal 
    // Employee.name, Employ
    


}





// Call function Similar to previous HW

// const init = () => {
//     promptUser()
//       .then((answers) => writeFileAsync('README-generator.md', generateREADME(answers)))
//       .then(() => console.log('Successfully wrote to README.md'))
//       .catch((err) => console.error(err));
//   };
  
//   init();